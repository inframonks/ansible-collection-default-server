---
# Rocky Linux specific variables (inherits from RedHat but can be customized)
certbot_packages:
  - certbot
  - python3-pip
  - python3-certbot-dns-cloudflare

# Package manager specific settings
certbot_package_manager: dnf
certbot_update_cache: false

# EPEL repository for additional packages
certbot_enable_epel: true
